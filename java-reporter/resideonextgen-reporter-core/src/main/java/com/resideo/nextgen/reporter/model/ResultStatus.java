package com.resideo.nextgen.reporter.model;

/** Mirrors the canonical status vocabulary used by the ResideoNextGen processor/frontend. */
public enum ResultStatus {
    PASSED,
    FAILED,
    BROKEN,
    SKIPPED,
    UNKNOWN
}
